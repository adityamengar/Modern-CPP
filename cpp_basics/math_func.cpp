#include <iostream>
#include<cmath>
using namespace std;

int main(){
    double x = 3.14;
    double y = 4;
    double z;

    //z=max(x,y);  //maximum
    //z=min(x,y); //minimum
    //z=pow(2,4); //power
    //z=sqrt(9); //square root
    //z=abs(-3); //absolute value nearest to zero how far is it so output is 3
    //z=round(x); //rounding off the number to nearest integer
    //z= ceil(x); //rounding up the number to nearest integer
    z=floor(x);
    cout<<z<<endl;
}

//we can find more C++ functions on website 
//cplusplus.com related to cmath library